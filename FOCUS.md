# Focus Doctor project file
# Edit freely, then run `focus use` in this folder to reload.
# Patterns match app name, window title, site or page title (case-insensitive); /regex/ allowed.

task: build Focus Doctor — the GNOME focus tracker in this repo (TypeScript daemon, shell extension, CLI)
allow: github.com, stackoverflow.com, gjs.guide, developer.gnome.org, ollama.com, nodejs.org
deny: 
