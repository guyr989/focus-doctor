{
  "name": "focus-doctord",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "focus-doctord": "dist/main.js",
    "focus": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/http/settings.html dist/http/ && cp src/classify/eval/cases.json dist/classify/eval/",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "pnpm build && node dist/main.js",
    "eval": "pnpm build && node dist/classify/eval/run.js"
  },
  "dependencies": {
    "dbus-next": "^0.10.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
