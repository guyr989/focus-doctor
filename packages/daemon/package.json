{
  "name": "focus-monitord",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "focus-monitord": "dist/main.js",
    "focus": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "node --experimental-strip-types --no-warnings src/main.ts"
  },
  "dependencies": {
    "dbus-next": "^0.10.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
