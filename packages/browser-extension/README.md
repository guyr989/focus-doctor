# Browser extension

Reports the **site name and page title** of your active tab to the daemon on `127.0.0.1:47113`. Full URLs never leave the browser. Works in Chrome and Brave.

Load it unpacked: `chrome://extensions` → enable *Developer mode* → *Load unpacked* → pick this folder.
